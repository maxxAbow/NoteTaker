const users = require('express').Router();
const User = require('../db/db.json');

//Create note
Notes.post('/', async (req,res)=> {
    if(noteTitle,noteBody){
        try {
            
        } catch (error) {
            res.status(500).send()
        }
    }
})